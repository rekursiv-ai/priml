"""Watching the game: sprites, a compositor, and a way to play it yourself.

Nothing here is on any training path. Import it to see what a policy did, or
to play the same world by hand and find out whether an achievement the agent
never unlocks is even reachable.

The sprites are downloaded on first use rather than committed, so installing
the package costs nothing until someone actually looks at a frame.
"""

from __future__ import annotations
