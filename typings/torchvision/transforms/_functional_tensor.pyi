from torch import Tensor

import torch

"""
This type stub file was generated by pyright.
"""

def get_dimensions(img: Tensor) -> list[int]: ...
def get_image_size(img: Tensor) -> list[int]: ...
def get_image_num_channels(img: Tensor) -> int: ...
def convert_image_dtype(
    image: torch.Tensor, dtype: torch.dtype = ...
) -> torch.Tensor: ...
def vflip(img: Tensor) -> Tensor: ...
def hflip(img: Tensor) -> Tensor: ...
def crop(img: Tensor, top: int, left: int, height: int, width: int) -> Tensor: ...
def rgb_to_grayscale(img: Tensor, num_output_channels: int = ...) -> Tensor: ...
def adjust_brightness(img: Tensor, brightness_factor: float) -> Tensor: ...
def adjust_contrast(img: Tensor, contrast_factor: float) -> Tensor: ...
def adjust_hue(img: Tensor, hue_factor: float) -> Tensor: ...
def adjust_saturation(img: Tensor, saturation_factor: float) -> Tensor: ...
def adjust_gamma(img: Tensor, gamma: float, gain: float = ...) -> Tensor: ...
def pad(
    img: Tensor,
    padding: int | list[int],
    fill: float | None = ...,
    padding_mode: str = ...,
) -> Tensor: ...
def resize(
    img: Tensor,
    size: list[int],
    interpolation: str = ...,
    antialias: bool | None = ...,
) -> Tensor: ...
def affine(
    img: Tensor,
    matrix: list[float],
    interpolation: str = ...,
    fill: float | list[float] | None = ...,
) -> Tensor: ...
def rotate(
    img: Tensor,
    matrix: list[float],
    interpolation: str = ...,
    expand: bool = ...,
    fill: float | list[float] | None = ...,
) -> Tensor: ...
def perspective(
    img: Tensor,
    perspective_coeffs: list[float],
    interpolation: str = ...,
    fill: float | list[float] | None = ...,
) -> Tensor: ...
def gaussian_blur(
    img: Tensor, kernel_size: list[int], sigma: list[float]
) -> Tensor: ...
def invert(img: Tensor) -> Tensor: ...
def posterize(img: Tensor, bits: int) -> Tensor: ...
def solarize(img: Tensor, threshold: float) -> Tensor: ...
def adjust_sharpness(img: Tensor, sharpness_factor: float) -> Tensor: ...
def autocontrast(img: Tensor) -> Tensor: ...
def equalize(img: Tensor) -> Tensor: ...
def normalize(
    tensor: Tensor, mean: list[float], std: list[float], inplace: bool = ...
) -> Tensor: ...
def erase(
    img: Tensor, i: int, j: int, h: int, w: int, v: Tensor, inplace: bool = ...
) -> Tensor: ...
def elastic_transform(
    img: Tensor,
    displacement: Tensor,
    interpolation: str = ...,
    fill: float | list[float] | None = ...,
) -> Tensor: ...
